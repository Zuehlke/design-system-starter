{
  "name": "@zuhlke/design-system",
  "private": true,
  "version": "1.0.0",
  "description": "Root project for the web-components monorepo",
  "scripts": {
    "check": "npm run check --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "analyze": "npm run analyze --workspaces --if-present",
    "version:major": "npm version major --workspaces",
    "version:minor": "npm version minor --workspaces",
    "version:patch": "npm version patch --workspaces"
  },
  "workspaces": [
    "tailwind-preset",
    "web-components",
    "web-components-react"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Zuehlke/design-system-starter"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@storybook/addon-designs": "^7.0.0",
    "storybook-addon-themes": "^6.1.0"
  }
}
